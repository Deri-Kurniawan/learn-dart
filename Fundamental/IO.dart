//rules to able use io function
import 'dart:io';

void main() {
  //question (output)
  stdout.write("Pilih Angka 1 - 100 > ");
  //required input with default String data type (input)
  print(stdin.readLineSync());
}
